# Pinned runtime environment.
#
# These are exact pins of the versions this project was developed and verified
# against on Python 3.14.7. Docker and CI install from this file.
#
# Refresh after a deliberate upgrade:
#   .venv/bin/python -m pip freeze | grep -v '^-e' | grep -v '^researcher'
#
# See requirements-dev.txt for test and quality tooling.

# --- Core runtime ----------------------------------------------------------
httpx==0.28.1
numpy==2.5.3
pydantic==2.13.5
pydantic-settings==2.15.0
tenacity==9.1.4
asyncpg==0.31.0

# --- LLM provider (synthesis) ----------------------------------------------
# Only the provider selected by LLM_PROVIDER is needed at runtime.
anthropic==1.5.0
# openai==2.6.1
# google-genai==2.14.0

# --- Web search provider ---------------------------------------------------
# Tavily and Serper are queried over plain httpx — no extra package required.
# Uncomment only when WEB_SEARCH_PROVIDER=duckduckgo.
# duckduckgo-search==8.1.1
