# Test and quality tooling. Not installed into the runtime Docker image.
#
#   .venv/bin/python -m pip install -r requirements-dev.txt

-r requirements.txt

# --- Testing -----------------------------------------------------------------
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
respx==0.23.1          # httpx transport mocking; keeps the suite offline

# --- Static analysis ---------------------------------------------------------
ruff==0.16.7
mypy==2.3.1

# --- Security scanning -------------------------------------------------------
pip-audit==2.10.1      # scans the runtime pins for known vulnerabilities
