[pytest]
asyncio_mode = strict

# Put the project root on sys.path so the top-level `ai` and `researcher`
# packages import even when pytest is invoked as the bare `pytest` console
# script. Unlike `python -m pytest`, the console script does not add the
# current working directory to sys.path on its own.
pythonpath = .
